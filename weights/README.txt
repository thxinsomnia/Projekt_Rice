Place your trained checkpoint here, e.g.:

    ProtoTypeEfficentNetB0CBAMV2Extra.pth

This matches DEFAULT_CHECKPOINT_PATH in config.py. Alternatively, upload
a .pth/.pt file directly from the app's sidebar at runtime.
